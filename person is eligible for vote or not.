#include <iostream>
using namespace std; 
void getperson()
{
	int age,r;
	cout<<"\nage = ";
	cin>>age;

  if(age >= 18)
    cout<<"eligible  ";
  else
    r=18-age;
    cout<<"not eligible";
    cout<<"\nyears left to eligible = "<<r;
}
int main()
{
    getperson();
	return 0;
}
